You're on velocity (HEAD 07871a6), all changes present. Since you're on Windows, use direct MT5 mode. Here's the restart + realtime test guide.

1. Pre-flight

- Both MT5 terminals running & logged in: Exness (feed) and MetaQuotes (execution).
- Kill any old daemon/dashboard still running (free port 8000).

2. Start — safest first (paper mode, no real orders)

Run in your terminal (the ! prefix runs it in this session so output lands here):
! cd /d D:\AXON.AI\AxonAgent-Agy && python run.py --direct --paper --symbol EURUSD
- --paper = simulated fills, never sends a live order — proves entry/exit/trail logic against live ticks with zero money risk. Start here.
- Dashboard auto-starts at http://127.0.0.1:8000 — open it in a browser.

3. What to verify on the dashboard

┌───────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────┐
│             Check             │                                 Where / what you're confirming                                  │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Ticks flowing                 │ Price updating live = Exness feed OK                                                            │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Velocity panel                │ z_score / decay_ratio / displacement populating (not stuck/null)                                │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ MarketContext quality scores  │ the panel you asked about earlier                                                               │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Economic-calendar strip       │ events load near SYSTEM button, color-coded                                                     │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Entry fires                   │ on momentum exhaustion → a paper position opens                                                 │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Trail behavior (the new code) │ watch SL: wide while move runs, tightens as momentum fades — NOT collapsing to ~1 pip on retest │
├───────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Sizing log                    │ lot ≈ (equity×1%)/(SL_pips×pip_value)                                                           │
└───────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────┘

4. Go live (real orders) — only after paper looks right

! cd /d D:\AXON.AI\AxonAgent-Agy && python run.py --direct --live --symbol EURUSD
⚠️ --live sends real orders to the MetaQuotes terminal. Confirm 1% sizing in the logs on the first trade before walking away.

5. Watching the trail specifically

In the daemon logs, grep for the new trail lines:
! cd /d D:\AXON.AI\AxonAgent-Agy && type logs\*.log | findstr "VelocityTrail"
You want to see agg/distance varying with momentum, and SL never jumping tighter than ~2 pips (the min_trail_floor_pips floor — the death-spiral fix).

If feed/exec paths differ, override with --feed-path / --exec-path. If ticks don't flow, the issue is almost always the Exness terminal not logged in or wrong feed path — not the code.

Want me to tail the daemon output here once you launch it, or check a specific panel?